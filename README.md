# Usage

```
hx-deploy-tool fetch-context         -- fetch missing or outdated context files from S3
hx-deploy-tool unpack 0.4.1          -- unpacks a release into the current directory
hx-deploy-tool select 0.4.1          -- stops the current release, unpacks the specified release, makes it current and starts it
```
